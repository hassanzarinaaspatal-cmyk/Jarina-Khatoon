name: mobile_app
description: Jarina Khatoon Flutter Application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Network & API
  http: 1.2.1
  
  # Local Storage
  shared_preferences: 2.5.5
  
  # UI
  cupertino_icons: 1.0.8
  
  # Media
  image_picker: 1.1.2
  file_picker: 6.2.1
  permission_handler: 12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true
